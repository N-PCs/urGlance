[package]
name = "urfileorganizer"
version = "0.1.0"
edition = "2024"

[dependencies]
cxx = "1.0"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "time", "sync"] }

[build-dependencies]
cxx-build = "1.0"
