[package]
name = "urglance"
version = "0.1.0"
edition = "2021"

[dependencies]
cxx = "1.0"
tokio = { version = "1", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
slint = "1.8.0"

[build-dependencies]
cxx-build = "1.0"
slint-build = "1.8.0"
